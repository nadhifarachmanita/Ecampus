<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nadifa.ecampus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityLogin" />
        <activity android:name=".MainActivitymenu" />
        <activity android:name=".MainActivityM_U" />
        <activity android:name=".MainActivityPTN" />
        <activity android:name=".MainActivityST" />
        <activity android:name=".MainActivityInst" />
        <activity android:name=".MainActivityAkademi" />
        <activity android:name=".MainActivityPTS"></activity>
    </application>

</manifest>